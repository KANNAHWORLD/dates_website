
// Styling for the navigation bar
export const navStyle = {
    padding: "1.5vh 2vw",
    margin: "auto auto auto auto",
    justifyContent: "center",
    height: "fit-content",  
}

// Styling for links on the webpage
export const linkStyle = {
    padding: "0vh 2vw",
    textDecoration: "none",
    color: "rgb(28, 72, 220)",
    fontSize: "large",
    fontWeight: "bold",
    fontFamily: "sans-serif",
}

// div container for the entire page
export const divStyle = {
    minHeight: "100vh",
    maxWidth: "100vw",
    padding: "0vh 0vw 0vh 0vw",
    backgroundColor: 'rgb(235, 244, 255)',
}

export const page_container = {
    textAlign: "center",
    maxHeight: "fit-content",
    padding: "0vh 0vw 5vh 0vw",
}

// Smaller div container for each section
export const divSection = {
    display: "flex",
    minHeight: "fit-content",
    justifyContent: "center",
    padding: "0vh 0vw 10vh 0vw",
}

export const titleStyle = {
    color: "rgb(28, 72, 220)",
    fontSize: "xx-large",
    fontWeight: "bold",
}

export const subTitleStyle = {
    color: "rgb(28, 72, 220)",
    fontSize: "20px",
}

export const paragraphStyle = {
    padding: "1vh 2vw",
    fontSize: "large",
    fontWeight: "5px",
    color: "rgb(28, 72, 220)",
}

export const headerStyle = {
    margin: "auto auto auto auto",
    height: "fit-content",
    backgroundColor: 'rgb(211, 228, 254)',
}

export const buttonStyle = {
    backgroundColor: "rgb(28, 72, 220)",
    color: "white",
    padding: "1vh 2vw",
    margin: "1vh 2vw",
    fontSize: "large",
    fontWeight: "bold",
    fontFamily: "sans-serif",
    borderRadius: "10px",
    border: "1px solid rgb(28, 72, 220)",
    textDecoration: "none",
    borderRadius: "10px",
    cursor: "pointer",
}

export const inverseButtonStyle = {
    backgroundColor: "transparent",
    color: "rgb(28, 72, 220)",
    padding: "1vh .5vw",
    margin: "1vh 2vw",
    fontSize: "large",
    fontWeight: "bold",
    fontFamily: "sans-serif",
    textDecoration: "none",
    borderRadius: "10px",
    cursor: "pointer",
    maxWidth: "fit-content",
}

export const cardStyle = {
    borderRadius: "10px",
    padding: "1vh 3vw",
    width: "18rem",
    borderTransparent: "true",
    minHeight: "fit-content",
    minWidth: "fit-content",
    boxShadow: "0px 2px 4px rgba(0, 0, 0, 0.1)",
    margin: "1vh 3vw",
}
